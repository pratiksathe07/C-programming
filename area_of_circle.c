#include <stdio.h>
int main()
{
    // 1.input feom user

    int radius;
    printf("Enter radius\n");
    scanf("%d",&radius);
    printf("The area of circle is %f",3.14*radius*radius);
    

    // // 2.intput in code

    // int r = 6;
    // printf("The area of circle with radius %d is %f\n",r,3.14*r*r);
    // return 0;
}